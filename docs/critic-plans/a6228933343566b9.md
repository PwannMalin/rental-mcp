# Critic plan `a6228933343566b9`

## Summary
Fix rental request search logic to filter by start date and respect customer context

## User input
ok can you list all the requests started this year

## Critic
```json
{
  "approve": false,
  "severity": "medium",
  "fixType": "logic",
  "needsCodeChange": true,
  "summary": "The assistant claims no rental requests started this year without showing any evidence of filtering or partial checks, potentially missing data or context.",
  "evidence": [
    "User asked to list all requests started this year.",
    "Assistant replied no results found without showing any filtering or partial data checks.",
    "No mention of using RENTAL entity with CustomerNumber or filtering by start date.",
    "No indication of checking multiple pages or partial results."
  ],
  "issues": [
    "No OData filter or query shown for requests started this year.",
    "No indication of customer context or use of CustomerNumber for rental request search.",
    "Assistant claims no results without clarifying if partial data was checked.",
    "Potential context loss if prior customer or request context existed."
  ],
  "requiredFixes": [
    "Implement proper filtering on RENTAL entity by start date for this year.",
    "Use CustomerNumber if customer context exists before searching RENTAL.",
    "If large data set, paginate and disclose partial checks.",
    "If no requests found after full check, clearly state that."
  ],
  "targetAreas": [
    "agent/copilotOrchestrator.js",
    "agent/rentalRequestHandler.js"
  ],
  "acceptanceCriteria": [
    "Assistant uses correct OData filters to find requests started this year.",
    "Assistant respects customer context and uses CustomerNumber for rental queries.",
    "Assistant discloses partial data checks if applicable.",
    "Assistant only claims no results after thorough search."
  ],
  "userFacingNote": null
}
```

## Architect plan
```json
{
  "canPatchCode": true,
  "branchName": "fix/critic-a62289",
  "prTitle": "Fix rental request search logic to filter by start date and respect customer context",
  "prBody": "This PR updates the rental request search logic to correctly filter rental requests started this year using OData filters on the start date. It also ensures that if a customer context with CustomerNumber exists, the search is scoped accordingly. Pagination is handled to check for partial results before concluding no results are found. This addresses the critique that the assistant previously claimed no results without proper filtering or context checks.",
  "files": [
    {
      "path": "src/agent/copilotOrchestrator.js",
      "instruction": "Update rental request search calls to include OData filters for start date within the current year and include CustomerNumber if present in context."
    },
    {
      "path": "src/agent/rentalRequestHandler.js",
      "instruction": "Enhance rental request query logic to apply OData filters for requests started this year, use CustomerNumber from context if available, implement pagination to check all pages, and only claim no results after full search."
    }
  ],
  "developerNotes": "Focus on adding correct OData filter expressions for start date (e.g., StartDate ge start of year and StartDate le end of year), use CustomerNumber from conversation state if available, and handle pagination to avoid premature no-result claims."
}
```

## Developer notes
Focus on adding correct OData filter expressions for start date (e.g., StartDate ge start of year and StartDate le end of year), use CustomerNumber from conversation state if available, and handle pagination to avoid premature no-result claims.