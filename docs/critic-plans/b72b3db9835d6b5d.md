# Critic plan `b72b3db9835d6b5d`

## Summary
Fix date range logic for 'requests created this year' query

## User input
how many requests have been created this year?

## Critic
```json
{
  "approve": false,
  "severity": "medium",
  "fixType": "logic",
  "needsCodeChange": true,
  "summary": "The assistant incorrectly reports no rental requests for 2026 without checking or disclosing partial data, and the date range is inconsistent with the current year.",
  "evidence": [
    "User asked for number of requests created this year (assumed current year 2024).",
    "Assistant replied with 'No rental requests found from 2026-01-01 through today.'",
    "The date range starts in 2026, which is in the future relative to now (2024).",
    "No indication that a partial check was done or that the date range was adjusted."
  ],
  "issues": [
    "The assistant uses a future date (2026-01-01) instead of the current year start date (e.g., 2024-01-01).",
    "Assistant claims no requests without clarifying if a partial check was done or if the data is complete.",
    "No OData filter or query is shown, but the response implies a date filter that is incorrect.",
    "The assistant does not handle the user's request properly by using the correct date range for 'this year'."
  ],
  "requiredFixes": [
    "Use the correct date range starting from January 1 of the current year (e.g., 2024-01-01) to today.",
    "If only a partial data check is done, disclose this to the user.",
    "Ensure the rental request query uses the correct OData filter for creation date within the current year.",
    "Do not invent or hallucinate dates or counts."
  ],
  "targetAreas": [
    "agent/orchestrator.js",
    "agent/filters.js"
  ],
  "acceptanceCriteria": [
    "When asked about requests created this year, the assistant uses the current year's start date in the filter.",
    "The assistant accurately reports the number of requests or discloses partial data checks.",
    "No future dates are used in queries or responses.",
    "The assistant does not claim no requests unless the data is fully checked or partial check is disclosed."
  ],
  "userFacingNote": null
}
```

## Architect plan
```json
{
  "canPatchCode": true,
  "branchName": "fix/critic-b72b3db9",
  "prTitle": "Fix date range logic for 'requests created this year' query",
  "prBody": "This PR fixes the date range used when filtering rental requests created in the current year. Previously, the code incorrectly used a future start date (2026-01-01), causing inaccurate or misleading responses. Now, the filter uses January 1 of the current year dynamically, up to today. This ensures correct OData filtering and accurate assistant replies without hallucinated dates or counts.",
  "files": [
    {
      "path": "src/agent/orchestrator.js",
      "instruction": "Update the date range filter logic for queries about requests created this year to use the current year start date (e.g., 2024-01-01) dynamically instead of a hardcoded future date."
    }
  ],
  "developerNotes": "Ensure the date calculation uses the system date to get the current year start date. Verify that the OData filter for creation date uses this range. Also, update any user-facing messages to avoid claiming no requests unless data is fully checked."
}
```

## Developer notes
Ensure the date calculation uses the system date to get the current year start date. Verify that the OData filter for creation date uses this range. Also, update any user-facing messages to avoid claiming no requests unless data is fully checked.