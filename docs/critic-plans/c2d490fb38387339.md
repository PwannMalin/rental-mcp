# Critic plan `c2d490fb38387339`

## Summary
Fix customer search to filter by CustomerName containing user query and search rental requests accordingly

## User input
Can you find any rental requests for amazon

## Critic
```json
{
  "approve": false,
  "severity": "medium",
  "fixType": "logic",
  "needsCodeChange": true,
  "summary": "The assistant incorrectly searched customers by index rather than filtering by customer name 'Amazon' and did not use the required OData filter for customer name search.",
  "evidence": [
    "User asked to find rental requests for 'amazon'.",
    "Assistant replied: 'Checked customers 1–25. None of them have open rental requests.'",
    "No indication that the assistant filtered customers by CustomerName containing 'Amazon'.",
    "No use of CUSTOMER filter with CustomerName as per domain rules.",
    "No rental request search by CustomerNumber after customer filtering."
  ],
  "issues": [
    "Customer search did not filter by CustomerName containing 'Amazon'.",
    "Assistant searched first 25 customers generically instead of narrowing to 'Amazon'.",
    "No rental request search performed after customer filtering.",
    "Pagination shown but no relevant filtering applied, leading to irrelevant results."
  ],
  "requiredFixes": [
    "Implement customer search using CUSTOMER filter with CustomerName containing 'Amazon'.",
    "After finding customers matching 'Amazon', retrieve their CustomerNumber(s).",
    "Use CustomerNumber(s) to search RENTAL requests.",
    "If multiple customers found, prompt user to select or paginate properly.",
    "Do not perform generic customer scans unrelated to the user's query."
  ],
  "targetAreas": [
    "customerSearch",
    "rentalRequestSearch",
    "paginationLogic"
  ],
  "acceptanceCriteria": [
    "When user asks for rental requests for 'amazon', the system filters customers by CustomerName containing 'Amazon'.",
    "Rental requests are searched using CustomerNumber(s) of matched customers.",
    "If multiple customers match, user is prompted to select or results are paginated properly.",
    "No generic customer scans unrelated to the query are performed.",
    "Pagination and user prompts reflect the filtered search context."
  ],
  "userFacingNote": null
}
```

## Architect plan
```json
{
  "canPatchCode": true,
  "branchName": "fix/critic-c2d490fb",
  "prTitle": "Fix customer search to filter by CustomerName containing user query and search rental requests accordingly",
  "prBody": "This PR fixes the customer search logic to apply an OData filter for CustomerName containing the user query (e.g., 'Amazon'). It then retrieves CustomerNumber(s) from matched customers and uses them to search rental requests. Pagination and user prompts are updated to reflect filtered search results instead of generic scans.",
  "files": [
    {
      "path": "src/agent/copilotOrchestrator.js",
      "instruction": "Update customer search logic to apply OData filter contains(CustomerName,'Amazon') when user query includes a customer name. After fetching matched customers, extract CustomerNumber(s) and use them to query rental requests. Adjust pagination and user prompts to reflect filtered results."
    }
  ],
  "developerNotes": "The fix focuses on correcting the search flow to respect the user's customer name filter and chaining rental request queries by CustomerNumber(s). No UI or external integration changes are needed."
}
```

## Developer notes
The fix focuses on correcting the search flow to respect the user's customer name filter and chaining rental request queries by CustomerNumber(s). No UI or external integration changes are needed.