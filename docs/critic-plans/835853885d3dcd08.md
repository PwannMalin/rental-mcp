# Critic plan `835853885d3dcd08`

## Summary
Fix customer search to filter by name and query rental requests accordingly

## User input
Can you find any rental requests for amazon

## Critic
```json
{
  "approve": false,
  "severity": "medium",
  "fixType": "logic",
  "needsCodeChange": true,
  "summary": "The assistant incorrectly searched customers by index range instead of filtering by customer name 'Amazon', and did not use the required OData filter for customer name search.",
  "evidence": [
    "User asked to find rental requests for 'amazon'.",
    "Assistant replied: 'Checked customers 1–25. None of them have open rental requests.'",
    "No mention of filtering customers by name containing 'Amazon'.",
    "No use of CUSTOMER filter with contains(CustomerName,'Amazon') as required.",
    "No rental requests search with CustomerNumber after customer identification."
  ],
  "issues": [
    "Customer search did not use the required filter contains(CustomerName,'Amazon').",
    "Assistant skipped the CUSTOMER step and directly checked customers by index range.",
    "No rental requests were searched using CustomerNumber after identifying the customer.",
    "Assistant response implies a generic scan of customers rather than a targeted search.",
    "No disclosure that only a subset of customers was checked in relation to 'Amazon'."
  ],
  "requiredFixes": [
    "Implement customer search using OData filter contains(CustomerName,'Amazon').",
    "After identifying customers matching 'Amazon', retrieve their CustomerNumber(s).",
    "Use CustomerNumber to query RENTAL for open rental requests.",
    "If multiple customers match 'Amazon', paginate results and allow narrowing by branch or other filters.",
    "Clearly disclose partial checks if not all customers are scanned."
  ],
  "targetAreas": [
    "agent/customerSearch.js",
    "agent/rentalRequestSearch.js",
    "agent/responseGeneration.js"
  ],
  "acceptanceCriteria": [
    "When user asks for rental requests for 'Amazon', the system filters customers by name containing 'Amazon'.",
    "Rental requests are searched using CustomerNumber(s) of matched customers.",
    "If multiple customers match, the system paginates and allows narrowing by branch.",
    "The assistant does not scan customers by arbitrary index ranges unrelated to the query.",
    "The assistant discloses if only a subset of customers or requests were checked."
  ],
  "userFacingNote": null
}
```

## Architect plan
```json
{
  "canPatchCode": true,
  "branchName": "fix/critic-8358538",
  "prTitle": "Fix customer search to filter by name and query rental requests accordingly",
  "prBody": "This PR updates the customer search logic to use OData filter contains(CustomerName,'Amazon') when the user queries rental requests for 'Amazon'. It then retrieves CustomerNumber(s) of matched customers and uses them to query rental requests. Pagination and branch filtering support is maintained. The assistant response is updated to disclose partial checks and avoid scanning by arbitrary index ranges unrelated to the query.",
  "files": [
    {
      "path": "src/agent/customerSearch.js",
      "instruction": "Modify customer search function to apply OData filter contains(CustomerName,'Amazon') when user input includes 'amazon'. Return matched customers with CustomerNumber."
    },
    {
      "path": "src/agent/rentalRequestSearch.js",
      "instruction": "Update rental request search to query by CustomerNumber(s) obtained from filtered customers instead of scanning by index range."
    },
    {
      "path": "src/agent/responseGeneration.js",
      "instruction": "Adjust response generation to disclose partial checks and avoid implying a generic scan by index range. Mention filtering by customer name and pagination."
    }
  ],
  "developerNotes": "The fix ensures the assistant uses proper OData filters for customer search and links rental requests to matched customers. It avoids scanning customers by index range unrelated to the query, improving accuracy and user experience."
}
```

## Developer notes
The fix ensures the assistant uses proper OData filters for customer search and links rental requests to matched customers. It avoids scanning customers by index range unrelated to the query, improving accuracy and user experience.