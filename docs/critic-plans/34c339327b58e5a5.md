# Critic plan `34c339327b58e5a5`

## Summary
Fix customer name extraction and search logic for rental requests

## User input
Can you find clampitt paper requests?

## Critic
```json
{
  "approve": false,
  "severity": "medium",
  "fixType": "logic",
  "needsCodeChange": true,
  "summary": "The assistant incorrectly searched for the entire user input as a customer name instead of extracting 'clampitt paper' as the customer search term.",
  "evidence": [
    "User asked: 'Can you find clampitt paper requests?'",
    "Assistant replied: 'I couldn't find any customers matching 'Can you find clampitt paper requests?''",
    "No tool was called or customer search performed with 'clampitt paper' as filter."
  ],
  "issues": [
    "The assistant did not parse the user input to extract the customer name 'clampitt paper'.",
    "The assistant treated the entire user input as a customer name filter, leading to no results.",
    "No attempt was made to perform a proper customer search or rental request retrieval.",
    "The assistant response is misleading and does not follow the domain rule to use CUSTOMER filter with CustomerName."
  ],
  "requiredFixes": [
    "Implement input parsing to extract customer name from user input.",
    "Use CUSTOMER filter with CustomerName containing 'clampitt paper' to search customers.",
    "If customers found, proceed to retrieve rental requests using CustomerNumber.",
    "Avoid searching with the entire user input as a filter."
  ],
  "targetAreas": [
    "agent/inputParsing.js",
    "agent/customerSearch.js",
    "agent/responseGeneration.js"
  ],
  "acceptanceCriteria": [
    "Given user input with a customer name embedded, the system extracts the customer name correctly.",
    "The system performs a customer search using CUSTOMER filter with CustomerName containing the extracted name.",
    "If customers are found, the system proceeds to retrieve rental requests using CustomerNumber.",
    "The system does not treat the entire user input as a customer name filter."
  ],
  "userFacingNote": null
}
```

## Architect plan
```json
{
  "canPatchCode": true,
  "branchName": "fix/critic-34c3393",
  "prTitle": "Fix customer name extraction and search logic for rental requests",
  "prBody": "This PR addresses the issue where the assistant incorrectly used the entire user input as a customer name filter. It implements minimal parsing to extract the customer name from the input and uses it to perform a proper customer search with a filter on CustomerName containing the extracted term. If customers are found, rental requests are retrieved using CustomerNumber. This ensures correct filtering and avoids misleading responses.",
  "files": [
    {
      "path": "src/agent/inputParsing.js",
      "instruction": "Add minimal logic to extract customer name from user input by identifying key phrases and isolating the customer name (e.g., extract 'clampitt paper' from 'Can you find clampitt paper requests?')."
    },
    {
      "path": "src/agent/customerSearch.js",
      "instruction": "Update customer search to use the extracted customer name with a filter like contains(CustomerName, '<extractedName>') instead of the entire user input."
    },
    {
      "path": "src/agent/responseGeneration.js",
      "instruction": "Modify response generation to avoid using the entire user input as a search term and instead reflect the extracted customer name in responses."
    }
  ],
  "developerNotes": "The fix focuses on minimal parsing to extract the customer name from the user input and updating the search logic accordingly. No major refactoring is done to keep the change minimal and targeted."
}
```

## Developer notes
The fix focuses on minimal parsing to extract the customer name from the user input and updating the search logic accordingly. No major refactoring is done to keep the change minimal and targeted.