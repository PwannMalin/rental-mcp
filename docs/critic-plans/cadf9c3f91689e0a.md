# Critic plan `cadf9c3f91689e0a`

## Summary
Add date filtering and pagination to rental request search

## User input
can you find rental requests made this year?

## Critic
```json
{
  "approve": false,
  "severity": "medium",
  "fixType": "logic",
  "needsCodeChange": true,
  "summary": "The assistant claims no rental requests found without showing any evidence of filtering or partial checks.",
  "evidence": [
    "User asked for rental requests made this year.",
    "Assistant replied no results found without showing any filtering or partial checks.",
    "No mention of filtering by date or checking for open requests.",
    "No indication of customer context or rental request filtering."
  ],
  "issues": [
    "No OData filter shown or implied for rental requests made this year.",
    "Assistant claims no results without clarifying if partial data was checked.",
    "No context of customer or rental request filtering, which is required by domain rules.",
    "No mention of checking open requests or pagination if large data sets exist."
  ],
  "requiredFixes": [
    "Implement filtering of rental requests by date (this year) using proper OData filters.",
    "If large data sets exist, implement pagination and disclose partial checks.",
    "Ensure rental requests are searched with CustomerNumber, not just date.",
    "Disclose partial checks if only a subset of requests is checked.",
    "Avoid claiming no results without confirming full or partial search."
  ],
  "targetAreas": [
    "agent/rentalRequestSearch.js",
    "agent/filteringLogic.js"
  ],
  "acceptanceCriteria": [
    "Rental requests are filtered by the current year using correct OData filters.",
    "If large data sets exist, pagination is used and partial checks are disclosed.",
    "Assistant does not claim no results without confirming search scope.",
    "Rental requests are searched using CustomerNumber context if available."
  ],
  "userFacingNote": null
}
```

## Architect plan
```json
{
  "canPatchCode": true,
  "branchName": "fix/critic-cadf9c3",
  "prTitle": "Add date filtering and pagination to rental request search",
  "prBody": "This PR updates the rental request search logic to filter requests by the current year using OData filters. It also adds pagination handling to manage large data sets and discloses when only partial results are shown. Additionally, the search now uses CustomerNumber context if available to scope the query appropriately. These changes address the critique about missing date filtering, incomplete search claims, and lack of context usage.",
  "files": [
    {
      "path": "src/agent/rentalRequestSearch.js",
      "instruction": "Add OData filter to include rental requests from the current year and use CustomerNumber if available. Implement pagination logic to fetch results in pages and indicate partial results if applicable."
    },
    {
      "path": "src/agent/filteringLogic.js",
      "instruction": "Update filtering logic to construct OData filters that include date range for the current year and CustomerNumber context. Ensure filters support pagination parameters."
    }
  ],
  "developerNotes": "Focus on adding a date filter using the current year (e.g., RentalDate ge 2024-01-01 and RentalDate le 2024-12-31) and include CustomerNumber in the filter if present. Implement pagination with skip and top parameters and handle partial result disclosure in the assistant's response."
}
```

## Developer notes
Focus on adding a date filter using the current year (e.g., RentalDate ge 2024-01-01 and RentalDate le 2024-12-31) and include CustomerNumber in the filter if present. Implement pagination with skip and top parameters and handle partial result disclosure in the assistant's response.