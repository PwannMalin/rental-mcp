# Critic plan `70d0b16fde30ba2f`

## Summary
Fix rental request query to include CustomerNumber filter and clarify search scope

## User input
no just query the request table and see what you find this year?

## Critic
```json
{
  "approve": false,
  "severity": "medium",
  "fixType": "logic",
  "needsCodeChange": true,
  "summary": "The assistant claims no requests found for the year 2023 without clarifying if the entire dataset was checked or only a subset, and it does not follow the required flow of filtering by customer number for rental requests.",
  "evidence": [
    "User asked to query the request table for this year.",
    "Assistant replied it found no requests from 2023.",
    "No mention of filtering by customer number or customer context.",
    "No indication of pagination or partial data check."
  ],
  "issues": [
    "Rental requests must be queried with CustomerNumber filter, not just by year alone.",
    "Assistant does not clarify if the search was comprehensive or partial.",
    "No mention of checking for open requests or pagination if large data set.",
    "Assistant response may mislead user by stating no results without clarifying scope."
  ],
  "requiredFixes": [
    "Implement filtering rental requests by CustomerNumber as per domain rules.",
    "If no customer context is available, clarify that customer search is needed first.",
    "If large data set, implement pagination and disclose partial checks.",
    "Avoid claiming no results without clarifying search scope."
  ],
  "targetAreas": [
    "agent/rentalRequestQuery.js",
    "agent/customerContextManagement.js"
  ],
  "acceptanceCriteria": [
    "Rental requests queries always include CustomerNumber filter.",
    "Assistant clarifies if only partial data checked or if no customer context is available.",
    "Pagination is used for large result sets and partial checks are disclosed.",
    "User is informed appropriately when no requests found with correct context."
  ],
  "userFacingNote": null
}
```

## Architect plan
```json
{
  "canPatchCode": true,
  "branchName": "fix/critic-70d0b16f",
  "prTitle": "Fix rental request query to include CustomerNumber filter and clarify search scope",
  "prBody": "This PR updates the rental request query logic to always include filtering by CustomerNumber as required by domain rules. It also adds checks for customer context availability and clarifies in the assistant's response whether the search was comprehensive or partial. Pagination handling is introduced for large result sets to avoid misleading claims of no results. These changes ensure the assistant provides accurate and context-aware responses when querying rental requests.",
  "files": [
    {
      "path": "src/agent/rentalRequestQuery.js",
      "instruction": "Modify the rental request query function to include CustomerNumber filter from the current customer context. Add pagination support for large result sets and return metadata indicating if the result is partial."
    },
    {
      "path": "src/agent/customerContextManagement.js",
      "instruction": "Add a function to check if a valid CustomerNumber is present in the current context. If not, the assistant should prompt the user to specify or search for a customer before querying rental requests."
    }
  ],
  "developerNotes": "The fix focuses on enforcing the domain rule that rental requests must be filtered by CustomerNumber. It also improves user communication by clarifying the scope of the search and handling pagination to avoid misleading 'no results' messages. This minimal fix does not overhaul the entire conversation flow but ensures compliance with critical query constraints."
}
```

## Developer notes
The fix focuses on enforcing the domain rule that rental requests must be filtered by CustomerNumber. It also improves user communication by clarifying the scope of the search and handling pagination to avoid misleading 'no results' messages. This minimal fix does not overhaul the entire conversation flow but ensures compliance with critical query constraints.