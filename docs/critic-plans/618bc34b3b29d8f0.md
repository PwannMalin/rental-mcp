# Critic plan `618bc34b3b29d8f0`

## Summary
Fix rental request search to use CustomerNumber from customer search

## User input
can you find amazon requests from any branch

## Critic
```json
{
  "approve": false,
  "severity": "medium",
  "fixType": "logic",
  "needsCodeChange": true,
  "summary": "The assistant incorrectly searched rental requests by customer name instead of using CustomerNumber, violating domain rules.",
  "evidence": [
    "User asked to find Amazon requests from any branch.",
    "Assistant searched rental requests with customer name containing 'Amazon'.",
    "Domain rules specify rental requests must use RENTAL with CustomerNumber, never CustomerName."
  ],
  "issues": [
    "Rental requests were searched using customer name filter instead of CustomerNumber.",
    "Assistant did not follow the required flow: CUSTOMER → CustomerNumber → RENTAL.",
    "Assistant did not clarify or paginate if multiple customers named Amazon exist."
  ],
  "requiredFixes": [
    "Implement a customer search by name 'Amazon' to retrieve CustomerNumber(s).",
    "Use the retrieved CustomerNumber(s) to search rental requests.",
    "If multiple customers match 'Amazon', paginate or ask user to select the correct one.",
    "Do not search rental requests directly by customer name."
  ],
  "targetAreas": [
    "agent/orchestrator.js",
    "agent/customerSearchHandler.js",
    "agent/rentalRequestHandler.js"
  ],
  "acceptanceCriteria": [
    "Customer search by name 'Amazon' returns CustomerNumber(s).",
    "Rental requests are searched only using CustomerNumber(s).",
    "If multiple customers match, the assistant asks user to select or paginates results.",
    "Assistant does not claim 'no results' without checking rental requests by CustomerNumber."
  ],
  "userFacingNote": null
}
```

## Architect plan
```json
{
  "canPatchCode": true,
  "branchName": "fix/critic-618bc34b",
  "prTitle": "Fix rental request search to use CustomerNumber from customer search",
  "prBody": "This PR fixes the rental request search logic to comply with domain rules by:\n\n- Searching customers by name (e.g., 'Amazon') to retrieve CustomerNumber(s).\n- Using CustomerNumber(s) to query rental requests instead of filtering by customer name.\n- Handling multiple customer matches by paginating or prompting user selection.\n\nThis ensures rental requests are correctly retrieved and prevents incorrect no-result claims.",
  "files": [
    {
      "path": "src/agent/orchestrator.js",
      "instruction": "Update orchestrator to chain customer search by name before rental request search, passing CustomerNumber(s) to rental request handler."
    },
    {
      "path": "src/agent/customerSearchHandler.js",
      "instruction": "Implement customer search by name returning CustomerNumber(s), support pagination or user selection if multiple matches."
    },
    {
      "path": "src/agent/rentalRequestHandler.js",
      "instruction": "Modify rental request search to accept CustomerNumber(s) and query rental requests accordingly, removing any customer name filters."
    }
  ],
  "developerNotes": "Minimal fix focuses on enforcing correct search flow CUSTOMER -> CustomerNumber -> RENTAL and avoids direct rental request search by customer name."
}
```

## Developer notes
Minimal fix focuses on enforcing correct search flow CUSTOMER -> CustomerNumber -> RENTAL and avoids direct rental request search by customer name.