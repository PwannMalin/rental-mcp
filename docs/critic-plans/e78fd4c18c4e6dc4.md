# Critic plan `e78fd4c18c4e6dc4`

## Summary
Fix filtering logic to apply location filter as narrowing step after customer name search

## User input
can you find any amazon rental requests for Houston

## Critic
```json
{
  "approve": false,
  "severity": "medium",
  "fixType": "logic",
  "needsCodeChange": true,
  "summary": "The assistant incorrectly combined customer name and location filters in a single search, violating domain rules.",
  "evidence": [
    "User asked for Amazon rental requests for Houston.",
    "Assistant searched for customer name containing 'Amazon' and location containing 'Houston' in one step.",
    "Domain rules specify that after an Amazon customer name search, a branch reply like 'Houston' must narrow that search, not start a new customer name search."
  ],
  "issues": [
    "The assistant combined 'Amazon' and 'Houston' filters in a single customer name search instead of narrowing the existing search.",
    "This violates the domain rule that location filters should narrow the existing customer search, not start a new one.",
    "Potentially missed rental requests due to incorrect filtering."
  ],
  "requiredFixes": [
    "Implement a two-step filtering process: first search customers with name containing 'Amazon', then narrow that search by location 'Houston'.",
    "Ensure the assistant uses CUSTOMER filter with 'Amazon' first, then applies location filter to narrow results.",
    "Avoid combining customer name and location filters in a single search."
  ],
  "targetAreas": [
    "agent/filteringLogic.js",
    "agent/customerSearchHandler.js"
  ],
  "acceptanceCriteria": [
    "When user requests 'Amazon' rental requests for 'Houston', the assistant first searches customers with name containing 'Amazon'.",
    "The assistant then narrows the search by location 'Houston' without restarting the customer name search.",
    "The assistant correctly identifies rental requests matching both criteria or reports none found after checking appropriately."
  ],
  "userFacingNote": null
}
```

## Architect plan
```json
{
  "canPatchCode": true,
  "branchName": "fix/critic-e78fd4c",
  "prTitle": "Fix filtering logic to apply location filter as narrowing step after customer name search",
  "prBody": "This PR addresses the issue where the assistant incorrectly combined customer name and location filters in a single search query. According to domain rules, after searching customers by name (e.g., 'Amazon'), a subsequent location filter (e.g., 'Houston') should narrow the existing search rather than restart or combine filters improperly.\n\nChanges:\n- Update filtering logic to perform a two-step filtering process.\n- First, search customers with name containing 'Amazon'.\n- Then, narrow the results by applying the location filter 'Houston'.\n\nThis ensures accurate search results and compliance with domain rules.",
  "files": [
    {
      "path": "src/agent/filteringLogic.js",
      "instruction": "Modify the function that builds the OData filter for rental requests to first apply the customer name filter, then apply the location filter as a narrowing step, avoiding combining them in a single filter clause."
    },
    {
      "path": "src/agent/customerSearchHandler.js",
      "instruction": "Adjust the customer search handler to support a two-step filtering process: initial customer name search followed by location-based narrowing, ensuring the search state is maintained correctly between steps."
    }
  ],
  "developerNotes": "The fix involves updating the filtering logic to respect domain rules about sequential filtering. Care should be taken to maintain conversation state so that location filters narrow existing customer searches rather than starting new ones."
}
```

## Developer notes
The fix involves updating the filtering logic to respect domain rules about sequential filtering. Care should be taken to maintain conversation state so that location filters narrow existing customer searches rather than starting new ones.