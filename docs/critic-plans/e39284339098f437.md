# Critic plan `e39284339098f437`

## Summary
Fix rental request year filter to use current year instead of hardcoded future year

## User input
how many requests have been made this year?

## Critic
```json
{
  "approve": false,
  "severity": "medium",
  "fixType": "logic",
  "needsCodeChange": true,
  "summary": "The assistant incorrectly reports no rental requests for the year 2026 without evidence of checking the correct date range or disclosing partial checks.",
  "evidence": [
    "User asked for the number of requests made this year.",
    "Assistant replied: 'No rental requests found from 2026-01-01 through today.'",
    "No tool summary or data was provided to confirm this.",
    "The year 2026 is in the future relative to the current date (2024)."
  ],
  "issues": [
    "The assistant uses an incorrect date range (2026) instead of the current year (2024).",
    "No indication that the assistant checked any data or partial data for requests.",
    "No disclosure of partial checks if applicable.",
    "Potential context loss or hallucination of date range."
  ],
  "requiredFixes": [
    "Use the correct current year date range (e.g., 2024-01-01 to today) when filtering rental requests.",
    "Ensure the assistant queries rental requests with proper OData filters for the current year.",
    "If only partial data is checked, disclose that to the user.",
    "Avoid hallucinating date ranges or data without backing from tool results."
  ],
  "targetAreas": [
    "agent/filteringLogic.js",
    "agent/dateHandling.js"
  ],
  "acceptanceCriteria": [
    "Assistant uses the current year date range when filtering rental requests.",
    "Assistant reports accurate counts or discloses partial checks.",
    "No hallucinated or future dates are used in responses.",
    "Responses align with actual tool data or clearly state lack of data."
  ],
  "userFacingNote": null
}
```

## Architect plan
```json
{
  "canPatchCode": true,
  "branchName": "fix/critic-e392843",
  "prTitle": "Fix rental request year filter to use current year instead of hardcoded future year",
  "prBody": "This patch updates the rental request filtering logic to use the current year dynamically instead of a hardcoded future year (2026). It ensures that OData filters for date ranges use the current year start date (e.g., 2024-01-01) through today, preventing hallucinated or incorrect date ranges. The assistant will now accurately query rental requests for the current year and report counts accordingly, avoiding misleading responses about future dates.\n\nFiles changed:\n- src/agent/filteringLogic.js: Adjust date range filter construction to use current year.\n- src/agent/dateHandling.js: Add utility to get current year start date dynamically.\n\nThis addresses the critique about incorrect date ranges and missing data checks, improving response accuracy and transparency.",
  "files": [
    {
      "path": "src/agent/filteringLogic.js",
      "instruction": "Modify the rental request OData filter to use a dynamic start date for the current year instead of a hardcoded 2026-01-01."
    },
    {
      "path": "src/agent/dateHandling.js",
      "instruction": "Add a utility function to return the start date of the current year (e.g., '2024-01-01') for use in filtering."
    }
  ],
  "developerNotes": "The fix is minimal and focused on correcting the date range used in rental request queries. It avoids hardcoded future years and ensures the assistant's responses align with actual data filtered by the current year. No changes to conversation state or tool argument handling are needed beyond date handling improvements."
}
```

## Developer notes
The fix is minimal and focused on correcting the date range used in rental request queries. It avoids hardcoded future years and ensures the assistant's responses align with actual data filtered by the current year. No changes to conversation state or tool argument handling are needed beyond date handling improvements.