# Critic plan `d52ce416c8da68c2`

## Summary
Fix customer and location filter logic for rental request searches

## User input
can you find any amazon rental requests for Houston

## Critic
```json
{
  "approve": false,
  "severity": "medium",
  "fixType": "logic",
  "needsCodeChange": true,
  "summary": "The assistant incorrectly combined customer name and location filters, violating the domain rule that location filters should narrow an existing customer name search rather than start a new one.",
  "evidence": [
    "User asked for Amazon rental requests for Houston.",
    "Assistant replied it searched for customer name containing 'Amazon' and location containing 'Houston' but found no results.",
    "Domain rules specify that after an Amazon (or other name) search, a branch reply like 'HOUSTON' must narrow that search (combined filter), not start a new CustomerName search for 'Houston'."
  ],
  "issues": [
    "Assistant combined customer name and location filters incorrectly, potentially causing a new search rather than narrowing the existing one.",
    "Assistant did not clarify or paginate customer search results before filtering rental requests.",
    "Assistant did not use the correct flow: CUSTOMER → CustomerNumber → RENTAL with CustomerNumber filter.",
    "Assistant did not mention checking for open requests or pagination if large customer sets exist."
  ],
  "requiredFixes": [
    "Implement a two-step search: first find customers with name containing 'Amazon', then narrow by location 'Houston' as a filter on that customer set.",
    "Use CUSTOMER search to get CustomerNumber(s) for 'Amazon', then use RENTAL with CustomerNumber filter to find rental requests.",
    "If multiple customers match 'Amazon', paginate or ask user to select before proceeding.",
    "Do not combine customer name and location in a single search filter incorrectly.",
    "Disclose partial checks if only a subset of requests is checked."
  ],
  "targetAreas": [
    "agent/copilotOrchestrator.js",
    "agent/searchHandler.js",
    "agent/filterLogic.js"
  ],
  "acceptanceCriteria": [
    "When user asks for 'Amazon' rental requests for 'Houston', the system first searches customers with name 'Amazon'.",
    "The system then narrows the search by location 'Houston' on the customer set, not as a new customer name search.",
    "Rental requests are searched using CustomerNumber(s) from the filtered customer set.",
    "If multiple customers match, the system paginates or asks for user selection before proceeding.",
    "The assistant response accurately reflects the search steps and any partial results."
  ],
  "userFacingNote": null
}
```

## Architect plan
```json
{
  "canPatchCode": true,
  "branchName": "fix/critic-d52ce41",
  "prTitle": "Fix customer and location filter logic for rental request searches",
  "prBody": "This PR addresses the incorrect combination of customer name and location filters in rental request searches. It implements a two-step search process:\n\n1. Search customers by name (e.g., 'Amazon') to get CustomerNumber(s).\n2. Narrow rental request search by location (e.g., 'Houston') using the CustomerNumber(s) filter.\n\nAdditionally, it adds pagination or user selection if multiple customers match the name filter before proceeding to rental requests.\n\nThis ensures compliance with domain rules and improves accuracy of search results and assistant responses.",
  "files": [
    {
      "path": "src/agent/copilotOrchestrator.js",
      "instruction": "Update search orchestration to first query CUSTOMER entity by name, then use resulting CustomerNumber(s) to filter RENTAL requests by location. Add pagination or user prompt if multiple customers match."
    },
    {
      "path": "src/agent/searchHandler.js",
      "instruction": "Modify search handler to separate customer name and location filters into sequential queries: first CUSTOMER search by name, then RENTAL search filtered by CustomerNumber(s) and location."
    },
    {
      "path": "src/agent/filterLogic.js",
      "instruction": "Adjust filter construction logic to prevent combining customer name and location filters in a single CUSTOMER search. Instead, apply location filter only when querying RENTAL requests filtered by CustomerNumber(s)."
    }
  ],
  "developerNotes": "The fix requires coordinating multiple components to implement the two-step search flow and handle multiple customer matches properly. Careful testing is needed to ensure the assistant's responses correctly reflect the search process and partial results."
}
```

## Developer notes
The fix requires coordinating multiple components to implement the two-step search flow and handle multiple customer matches properly. Careful testing is needed to ensure the assistant's responses correctly reflect the search process and partial results.