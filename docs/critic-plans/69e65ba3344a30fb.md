# Critic plan `69e65ba3344a30fb`

## Summary
Fix customer search logic to use proper contains filter for customer names

## User input
do we have any requests for clampitt paper?

## Critic
```json
{
  "approve": false,
  "severity": "medium",
  "fixType": "logic",
  "needsCodeChange": true,
  "summary": "The assistant incorrectly searched for customers with a malformed filter and did not follow domain rules for customer name search.",
  "evidence": [
    "User asked for requests for 'clampitt paper'.",
    "Assistant replied: \"I searched for customers named 'clampitt ?' but found none.\"",
    "Domain rules specify customer name search uses CUSTOMER with contains(CustomerName,'Amazon') style filter.",
    "Assistant did not use correct filter or clarify the search properly."
  ],
  "issues": [
    "The assistant used an incorrect or malformed search filter ('clampitt ?') instead of a proper contains filter.",
    "The assistant did not attempt to search customers using the correct OData filter for CustomerName containing 'clampitt paper'.",
    "The assistant did not clarify or ask for more information given the ambiguous input.",
    "The assistant did not follow the flow to find customers first, then rental requests.",
    "The assistant's reply is misleading as it claims no customers found without proper search."
  ],
  "requiredFixes": [
    "Implement correct customer name search using contains(CustomerName,'clampitt paper') filter.",
    "If multiple customers found, prompt user to select one.",
    "If one customer found, proceed to check rental requests for that customer.",
    "If no customers found, clearly state no customers matched 'clampitt paper'.",
    "Avoid malformed or partial filters like 'clampitt ?'."
  ],
  "targetAreas": [
    "src/agent/customerLookup.js",
    "src/agent/requestFlow.js"
  ],
  "acceptanceCriteria": [
    "When user asks for requests for 'clampitt paper', the system searches customers with contains(CustomerName,'clampitt paper').",
    "If customers found, user is prompted to select or system auto-continues if only one.",
    "If no customers found, system clearly states no matching customers found.",
    "No malformed or partial filters are used in customer search."
  ],
  "userFacingNote": null
}
```

## Architect plan
```json
{
  "canPatchCode": true,
  "branchName": "fix/critic-69e65ba3",
  "prTitle": "Fix customer search logic to use proper contains filter for customer names",
  "prBody": "This PR fixes the customer search logic to correctly use the OData filter contains(CustomerName,'searchTerm') when searching for customers by name. It replaces the malformed or partial filters such as 'clampitt ?' with the proper contains filter.\n\n- Uses customerLookup.js to implement correct contains filter for customer name search.\n- Updates requestFlow.js to handle the flow: if multiple customers found, prompt user to select; if one found, proceed to rental requests; if none found, clearly state no matches.\n\nThis ensures the assistant properly searches customers and follows domain rules, improving accuracy and user experience.",
  "files": [
    {
      "path": "src/agent/customerLookup.js",
      "instruction": "Fix searchCustomersFromText to build OData filter using contains(CustomerName,'searchTerm') correctly without malformed filters."
    },
    {
      "path": "src/agent/requestFlow.js",
      "instruction": "Update flow to handle multiple/single/no customer results properly: prompt selection if multiple, proceed if single, notify if none."
    }
  ],
  "developerNotes": "The main fix is to ensure customerLookup.js builds the correct OData filter with contains(CustomerName,'searchTerm') and requestFlow.js uses that result properly to prompt or continue. No changes to copilotOrchestrator.js needed."
}
```

## Developer notes
The main fix is to ensure customerLookup.js builds the correct OData filter with contains(CustomerName,'searchTerm') and requestFlow.js uses that result properly to prompt or continue. No changes to copilotOrchestrator.js needed.