# Critic plan `0bbe1252d92710f0`

## Summary
Enforce customer context before rental request search

## User input
can you look up any request that has happened in the past month?

## Critic
```json
{
  "approve": false,
  "severity": "medium",
  "fixType": "logic",
  "needsCodeChange": true,
  "summary": "The assistant fails to filter rental requests by customer number and does not clarify the customer context before searching, violating domain rules.",
  "evidence": [
    "User asked to look up any request in the past month without specifying a customer.",
    "Assistant replied with a generic search for rental requests in the past month with no results.",
    "No mention of customer search or filtering by CustomerNumber in the draft answer.",
    "Domain rules require rental requests to be searched using CUSTOMER to get CustomerNumber first, then RENTAL with CustomerNumber."
  ],
  "issues": [
    "Assistant did not perform a customer search before looking up rental requests.",
    "Assistant did not filter rental requests by CustomerNumber as required.",
    "Assistant gave a generic 'no results' answer without clarifying or requesting customer information.",
    "Potential context loss or ignoring domain flow: CUSTOMER → CustomerNumber → RENTAL."
  ],
  "requiredFixes": [
    "Implement a customer search step if no customer context is active.",
    "Use CUSTOMER filter to find CustomerNumber before querying RENTAL.",
    "If multiple customers found, prompt user to select one or paginate results.",
    "Only after selecting a customer, search rental requests filtered by CustomerNumber and date range.",
    "If no requests found, disclose how many were checked if partial."
  ],
  "targetAreas": [
    "agent/copilotOrchestrator.js",
    "agent/customerSearch.js",
    "agent/rentalRequestSearch.js"
  ],
  "acceptanceCriteria": [
    "When user asks for requests without specifying customer, system asks for or searches customers first.",
    "Rental requests are always filtered by CustomerNumber, never by CustomerName alone.",
    "If no requests found, system clearly states the scope of search and any pagination.",
    "User is guided through customer selection if multiple matches exist."
  ],
  "userFacingNote": null
}
```

## Architect plan
```json
{
  "canPatchCode": true,
  "branchName": "fix/critic-0bbe12",
  "prTitle": "Enforce customer context before rental request search",
  "prBody": "This PR fixes the logic to ensure rental requests are always searched with a valid CustomerNumber filter. When the user query lacks customer context, the assistant now performs a customer search first, prompts for selection if multiple customers are found, and only then queries rental requests filtered by CustomerNumber and date range. The assistant also clarifies the search scope and pagination status in the response when no rental requests are found. This aligns with domain rules and improves user guidance.",
  "files": [
    {
      "path": "src/agent/copilotOrchestrator.js",
      "instruction": "Update orchestrator logic to check for active customer context before rental request search; if missing, trigger customer search flow first."
    },
    {
      "path": "src/agent/customerSearch.js",
      "instruction": "Enhance customer search to support prompting user for selection when multiple customers match."
    },
    {
      "path": "src/agent/rentalRequestSearch.js",
      "instruction": "Modify rental request search to always filter by CustomerNumber and date range; update responses to clarify search scope and pagination."
    }
  ],
  "developerNotes": "Minimal fix focuses on enforcing customer context and filtering by CustomerNumber before rental request queries, improving flow and compliance with domain rules."
}
```

## Developer notes
Minimal fix focuses on enforcing customer context and filtering by CustomerNumber before rental request queries, improving flow and compliance with domain rules.