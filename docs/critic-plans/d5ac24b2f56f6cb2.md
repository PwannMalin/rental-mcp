# Critic plan `d5ac24b2f56f6cb2`

## Summary
Fix customer name extraction and search filter for request queries

## User input
can you find me requests from clampitt paper

## Critic
```json
{
  "approve": false,
  "severity": "medium",
  "fixType": "logic",
  "needsCodeChange": true,
  "summary": "The assistant incorrectly used the entire user input as a customer search term instead of extracting the customer name 'clampitt paper'.",
  "evidence": [
    "User input: 'can you find me requests from clampitt paper'",
    "Assistant response: 'I couldn't find any customers matching 'can you find me requests from clampitt paper''",
    "No customer search was done using the correct filter for 'clampitt paper'"
  ],
  "issues": [
    "The assistant did not extract the customer name 'clampitt paper' from the user input and used the full sentence as the search term.",
    "This caused a failed customer search and a poor user experience.",
    "The assistant did not proceed to find requests for the identified customer."
  ],
  "requiredFixes": [
    "Implement logic to parse and extract the customer name from the user input.",
    "Use the extracted customer name in the customer search filter (e.g., contains(CustomerName,'clampitt paper')).",
    "If a single customer is found, proceed to retrieve rental requests using the CustomerNumber.",
    "Avoid using the entire user input as a search term."
  ],
  "targetAreas": [
    "agent/nlpParsing.js",
    "agent/customerSearchHandler.js",
    "agent/requestRetrieval.js"
  ],
  "acceptanceCriteria": [
    "Given a user input containing a request for customer requests, the system extracts the customer name correctly.",
    "The system performs a customer search using the extracted name with the correct OData filter.",
    "If one customer matches, the system retrieves and displays the rental requests for that customer.",
    "The assistant does not use the entire user input as a search term."
  ],
  "userFacingNote": null
}
```

## Architect plan
```json
{
  "canPatchCode": true,
  "branchName": "fix/critic-d5ac24b",
  "prTitle": "Fix customer name extraction and search filter for request queries",
  "prBody": "This PR addresses the issue where the entire user input was incorrectly used as the customer search term instead of extracting the customer name. It updates the NLP parsing logic to extract the customer name from user input when requesting customer requests, applies the correct OData filter using contains(CustomerName, '<extractedName>'), and ensures that if a single customer is found, rental requests are retrieved using the CustomerNumber. This improves search accuracy and user experience.",
  "files": [
    {
      "path": "src/agent/nlpParsing.js",
      "instruction": "Add or improve logic to parse and extract the customer name from user input when the user requests customer requests."
    },
    {
      "path": "src/agent/customerSearchHandler.js",
      "instruction": "Update customer search to use the extracted customer name with contains(CustomerName, '<extractedName>') filter instead of the full user input."
    },
    {
      "path": "src/agent/requestRetrieval.js",
      "instruction": "Ensure that if a single customer is found, rental requests are retrieved using the CustomerNumber."
    }
  ],
  "developerNotes": "Focus on minimal changes to parsing and search filter logic to correctly extract and use customer names from user input. Avoid broad NLP changes; target only the extraction relevant to customer request queries."
}
```

## Developer notes
Focus on minimal changes to parsing and search filter logic to correctly extract and use customer names from user input. Avoid broad NLP changes; target only the extraction relevant to customer request queries.